"""Ground-side weather pipeline: A (collect) -> B (process) -> thermal prior.

No onboard sensors involved — this is the strategic layer that turns real weather
into the glider's pre-flight thermal map + wind.
"""
